{
  "name": "lic-fullstack",
  "version": "1.0.0",
  "description": "LIC Insurance Management System",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npx -y concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
